nav.navbar(role="navigation" aria-label="main navigation")
  a.navbar-item(title=site.name href=url)
    include ../../../assets/imgs/pyphoy.svg
  if home
    a.navbar-item.site-name(title=site.name href=url)= site.name
  else
    .navbar-item
      if pypData
        +selectCity(pypData.name)
        +selectCategory(pypData.path, categoryName ? pypData.data[0].categories[0].name : false)
      else
        +selectCity()

    
header.header.container.text-center
  h1.title= title || site.name
  .subtitle
    time(datetime=ISODate)= date
  if archive
    div.archive
      +archiveWarning()
  if pagePath.includes("armenia")
    div.archive Mediante decreto 313 del 28 de diciembre del 2018 se levanta la medida para particulares y motos del 1 al 8 de enero de 2019.
.container.text-center
  if pagePath
    +breadCrumbs(pagePath)
