extends layouts/layout

block variables
  - const home = true

block pypData
  section.hero
    .container.text-center
      h1.title= site.name
      h2.subtitle
        time(datetime=ISODate)= date
    .container.text-center
      +selectCity('', true)
